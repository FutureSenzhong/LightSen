### 1.先在本地git清除文件夹
```
# 查看本地版本库里的文件
    git status
# 删除多余的文件
　　git rm xxx(xxx表文件名)  -rf ( -r 当给出主目录名时允许递归删除)
```
### 2.本地提交更改
```
# 提交更新
　　git commit -m''(-m 备注)
```

### 3.最后push到远程仓库
```
# 更新远程仓库内容
　　git push 
```
最后查看远程仓库内容是否和本地一致